@model Agro.Model.Dto.Request.RequestDto

<div>
    <h4>Запрос поставщику</h4>
    <hr />
    <dl class="row">        
        <dt class="col-sm-2">
            Город
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.City.Name)
        </dd>
        

    </dl>
  Контакты
  
  @foreach (var intention in Model.Intentions)
  {
    <div class="col-md-10">контактный номер @intention.User.PhoneNumber <a asp-controller="intentions" asp-action="details" asp-route-id="@intention.Id">Заявка потребителя</a></div>

    
  }
</div>
<div>    
    <a asp-action="Index">К списку запросов</a>
</div>

